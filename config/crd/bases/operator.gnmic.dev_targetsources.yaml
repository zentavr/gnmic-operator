---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: targetsources.operator.gnmic.dev
spec:
  group: operator.gnmic.dev
  names:
    kind: TargetSource
    listKind: TargetSourceList
    plural: targetsources
    singular: targetsource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TargetSource is the Schema for the targetsources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetSourceSpec defines the desired state of TargetSource
            properties:
              provider:
                properties:
                  consul:
                    properties:
                      url:
                        minLength: 1
                        type: string
                    type: object
                  http:
                    properties:
                      acceptPush:
                        type: boolean
                      url:
                        minLength: 1
                        type: string
                    required:
                    - url
                    type: object
                type: object
                x-kubernetes-validations:
                - message: exactly one of the fields in [http consul] must be set
                  rule: '[has(self.http),has(self.consul)].filter(x,x==true).size()
                    == 1'
              targetLabels:
                additionalProperties:
                  type: string
                type: object
              targetProfile:
                minLength: 1
                type: string
            required:
            - provider
            - targetProfile
            type: object
          status:
            description: TargetSourceStatus defines the observed state of TargetSource
            properties:
              lastSync:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              status:
                type: string
              targetsCount:
                format: int32
                type: integer
            required:
            - observedGeneration
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
